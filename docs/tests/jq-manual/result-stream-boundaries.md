---
type: Report
title: "jq manual: result-stream-boundaries token comparison"
description: Generated jq and tq output comparisons and token counts.
generated: { by: "tq-manual-compare/0.1.0", at: "2026-09-10T20:22:34.585009Z" }
---

# jq manual: result-stream-boundaries token comparison

<!-- tq-manual-compare:begin section=result-stream-boundaries -->
## Results

[Case collection](../../../tests/compatibility/reviews/jq-manual/result-stream-boundaries.toon)


| Verdict | Cases |
| --- | ---: |
| match | 23 |

Independent output campaigns must pass too. Compact JSON compares exact stdout bytes and process behavior; TOON compares ordered values and process behavior with the JSON execution.

| Output campaign | Matches | Cases |
| --- | ---: | ---: |
| compact_json | 21 | 21 |
| toon | 21 | 21 |

A match requires equivalent JSON results and process behavior, or a matching non-JSON CLI contract. Reviewed disparities retain exact observations and count separately from matches. Historical expected-difference labels do not pass either gate.

Missing features and unaccepted mismatches remain failures. Reference discrepancies describe errors in the imported manual, not successful compatibility.

JSON equivalence ignores whitespace and object key order but retains array and result-sequence order. Error-only cases do not count as JSON matches or size samples. Raw CLI cases keep their original arguments and have no JSON/TOON size measurement.

### Output size

9 eligible examples. Counts use the `o200k_base` and `cl100k_base` tokenizers over complete stdout, including trailing newlines. The totals compare default `-o json` output with default LF-terminated `-o toon` results; explicitly requested `--seq -o toon` output is shown in the cases but excluded from size totals. Diff is TOON tokens minus JSON tokens. % is the signed percent difference `(TOON - JSON) / JSON`, so savings are negative and growth is positive.

| Tokenizer | JSON tokens | TOON tokens | Diff | % |
| --- | ---: | ---: | ---: | ---: |
| `o200k_base` | 48 | 44 | -4 | -8.33% |
| `cl100k_base` | 48 | 44 | -4 | -8.33% |

Only successful jq/JSON/TOON-equivalent results enter the totals. A negative `Diff` means TOON uses fewer tokens; `%` is negative for savings and positive for growth. The manual is a correctness corpus, not a representative workload benchmark.

### Reviewed disparities and historical differences

| Case | Reason |
| --- | --- |

### Cases

Each case shows the original jq invocation, then the complete jq, tq JSON, and tq TOON output. Control bytes use `\xNN` escapes so record separators remain visible. This section is generated by the separate `tq-manual-compare` command.

#### manual.audit.streaming.json-root-boundary-ancestor-remove

```
# input
jq '.outer.features[].id'

# jq


[stderr]
jq: error (at <stdin>:0): Cannot iterate over null (null)

# tq -o json


[stderr]
tq: runtime error: iterate cannot be applied to null

# tq


[stderr]
tq: runtime error: iterate cannot be applied to null
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-array-ancestor-type

```
# input
jq '.items[1].features[].id'

# jq


[stderr]
jq: error (at <stdin>:0): Cannot index number with string "features"

# tq -o json


[stderr]
tq: runtime error: field access cannot be applied to number

# tq


[stderr]
tq: runtime error: field access cannot be applied to number
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-auto

```
# input
jq '.features[].id'

# jq
1
2

# tq -o json
1
2

# tq
1
2
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 4 | 4 | 4 | 0 | +0% |
| `cl100k_base` | 4 | 4 | 4 | 0 | +0% |

#### manual.audit.streaming.json-root-boundary-duplicate-key

```
# input
jq '.features[].id'

# jq

# tq -o json

# tq
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-explicit

```
# input
jq '.features[].id'

# jq
1
2

# tq -o json
1
2

# tq
1
2
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 4 | 4 | 4 | 0 | +0% |
| `cl100k_base` | 4 | 4 | 4 | 0 | +0% |

#### manual.audit.streaming.json-root-boundary-hybrid

```
# input
jq '[.items[].value] | sort'

# jq
[
  1,
  2
]
[
  3,
  4
]

# tq -o json
[
  1,
  2
]
[
  3,
  4
]

# tq
[2]: 1,2
[2]: 3,4
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 20 | 20 | 16 | -4 | -20% |
| `cl100k_base` | 20 | 20 | 16 | -4 | -20% |

#### manual.audit.streaming.json-root-boundary-nested-empty

```
# input
jq '.features[].item | debug'

# jq
{}


[stderr]
["DEBUG:",{}]

# tq -o json
{}


[stderr]
["DEBUG:",{}]

# tq



[stderr]
["DEBUG:",{}]
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 1 | 1 | 1 | 0 | +0% |
| `cl100k_base` | 1 | 1 | 1 | 0 | +0% |

#### manual.audit.streaming.json-root-boundary-no-partial

```
# input
jq '.features[].id | debug'

# jq


[stderr]
jq: parse error: Expected another array element at line 1, column 48

# tq -o json


[stderr]
tq: Json input rejected: invalid JSON at byte 47, line 1, column 48: expected JSON value

# tq


[stderr]
tq: Json input rejected: invalid JSON at byte 47, line 1, column 48: expected JSON value
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-no-vm

```
# input
jq '.features[].id | error("must-not-run")'

# jq


[stderr]
jq: parse error: Expected another array element at line 1, column 48

# tq -o json


[stderr]
tq: Json input rejected: invalid JSON at byte 47, line 1, column 48: expected JSON value

# tq


[stderr]
tq: Json input rejected: invalid JSON at byte 47, line 1, column 48: expected JSON value
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-null-prefix

```
# input
jq '.features[].id'

# jq


[stderr]
jq: error (at <stdin>:0): Cannot iterate over null (null)

# tq -o json


[stderr]
tq: runtime error: iterate cannot be applied to null

# tq


[stderr]
tq: runtime error: iterate cannot be applied to null
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-object-order

```
# input
jq '.features[].id'

# jq
3
2

# tq -o json
3
2

# tq
3
2
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 4 | 4 | 4 | 0 | +0% |
| `cl100k_base` | 4 | 4 | 4 | 0 | +0% |

#### manual.audit.streaming.json-root-boundary-prefix-reset

```
# input
jq '.features[].id'

# jq
1
2
3

# tq -o json
1
2
3

# tq
1
2
3
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 6 | 6 | 6 | 0 | +0% |
| `cl100k_base` | 6 | 6 | 6 | 0 | +0% |

#### manual.audit.streaming.json-root-boundary-prior-valid

```
# input
jq '.features[].id'

# jq
1


[stderr]
jq: parse error: Expected another array element at line 1, column 63

# tq -o json
1


[stderr]
tq: Json input rejected: invalid JSON at byte 62, line 1, column 63: expected JSON value

# tq
1


[stderr]
tq: Json input rejected: invalid JSON at byte 62, line 1, column 63: expected JSON value
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 2 | 2 | 2 | 0 | +0% |
| `cl100k_base` | 2 | 2 | 2 | 0 | +0% |

#### manual.audit.streaming.json-root-boundary-root-array

```
# input
jq '.features[].id'

# jq


[stderr]
jq: error (at <stdin>:0): Cannot index array with string "features"

# tq -o json


[stderr]
tq: runtime error: field access cannot be applied to array

# tq


[stderr]
tq: runtime error: field access cannot be applied to array
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-root-empty-array

```
# input
jq '.features[].id'

# jq


[stderr]
jq: error (at <stdin>:0): Cannot index array with string "features"

# tq -o json


[stderr]
tq: runtime error: field access cannot be applied to array

# tq


[stderr]
tq: runtime error: field access cannot be applied to array
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-root-scalar

```
# input
jq '.features[].id'

# jq


[stderr]
jq: error (at <stdin>:0): Cannot index number with string "features"

# tq -o json


[stderr]
tq: runtime error: field access cannot be applied to number

# tq


[stderr]
tq: runtime error: field access cannot be applied to number
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.audit.streaming.json-root-boundary-runtime-order

```
# input
jq 'error("vm-marker")'

# jq


[stderr]
jq: error (at <stdin>:0): vm-marker
jq: parse error: Expected another array element at line 1, column 20

# tq -o json


[stderr]
tq: runtime error: vm-marker
tq: Json input rejected: invalid JSON at byte 19, line 1, column 20: expected JSON value

# tq


[stderr]
tq: runtime error: vm-marker
tq: Json input rejected: invalid JSON at byte 19, line 1, column 20: expected JSON value
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.runtime.continuation.effect-order

```
# input
jq 'if . == 1 then error("first") elif . == 2 then debug("middle") else error("last") end'

# jq
2


[stderr]
jq: error (at <stdin>:1): first
["DEBUG:","middle"]
jq: error (at <stdin>:3): last

# tq -o json
2


[stderr]
tq: runtime error: first
["DEBUG:","middle"]
tq: runtime error: last

# tq
2


[stderr]
tq: runtime error: first
["DEBUG:","middle"]
tq: runtime error: last
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 2 | 2 | 2 | 0 | +0% |
| `cl100k_base` | 2 | 2 | 2 | 0 | +0% |

#### manual.runtime.continuation.empty-exit

```
# input
jq -e 'if type == "string" then . + 1 else empty end'

# jq


[stderr]
jq: error (at <stdin>:1): string ("x") and number (1) cannot be added

# tq -o json


[stderr]
tq: runtime error: cannot add string and number

# tq
<not run>
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | n/a | n/a | n/a |
| `cl100k_base` | 0 | 0 | n/a | n/a | n/a |

#### manual.runtime.continuation.halt-stops

```
# input
jq 'if . == 1 then halt_error(7) else . end'

# jq


[stderr]
1

# tq -o json


[stderr]
1

# tq
<not run>
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | n/a | n/a | n/a |
| `cl100k_base` | 0 | 0 | n/a | n/a | n/a |

#### manual.runtime.continuation.intermediate-error

```
# input
jq '. + 1'

# jq
2
3


[stderr]
jq: error (at <stdin>:2): string ("x") and number (1) cannot be added

# tq -o json
2
3


[stderr]
tq: runtime error: cannot add string and number

# tq
2
3


[stderr]
tq: runtime error: cannot add string and number
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 4 | 4 | 4 | 0 | +0% |
| `cl100k_base` | 4 | 4 | 4 | 0 | +0% |

#### manual.runtime.continuation.parse-after-error

```
# input
jq '. + 1'

# jq


[stderr]
jq: error (at <stdin>:1): string ("x") and number (1) cannot be added
jq: parse error: Unfinished JSON term at EOF at line 3, column 0

# tq -o json


[stderr]
tq: runtime error: cannot add string and number
tq: Json input rejected: invalid JSON at byte 6, line 3, column 1: expected object key or '}'

# tq


[stderr]
tq: runtime error: cannot add string and number
tq: Json input rejected: invalid JSON at byte 6, line 3, column 1: expected object key or '}'
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 0 | 0 | 0 | 0 | n/a |
| `cl100k_base` | 0 | 0 | 0 | 0 | n/a |

#### manual.runtime.continuation.raw-lines

```
# input
jq -R -c 'if . == "x" then error("bad") else . end'

# jq
"a"
"b"


[stderr]
jq: error (at <stdin>:2): bad

# tq -o json
"a"
"b"


[stderr]
tq: runtime error: bad

# tq
"a"
"b"


[stderr]
tq: runtime error: bad
```

| Tokenizer | jq | tq -o json | tq | Diff | % |
| --- | ---: | ---: | ---: | ---: | ---: |
| `o200k_base` | 5 | 5 | 5 | 0 | +0% |
| `cl100k_base` | 5 | 5 | 5 | 0 | +0% |

<!-- tq-manual-compare:end -->
